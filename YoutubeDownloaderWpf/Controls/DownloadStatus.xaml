<UserControl x:Class="YoutubeDownloaderWpf.Controls.DownloadStatus"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:YoutubeDownloaderWpf.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" MinHeight="30">
    <Grid Background="{Binding Background}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="50"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Text="{Binding Name}" HorizontalAlignment="Left"></TextBlock>
        <TextBlock Grid.Column="1" Text="{Binding Size}" HorizontalAlignment="Left"></TextBlock>
        <CheckBox Grid.Column="2" IsChecked="{Binding IsCompleted,Mode=OneWay}" IsEnabled="False"></CheckBox>
    </Grid>
</UserControl>
